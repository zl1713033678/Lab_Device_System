# Lab Device System 协作规约

本仓库采用“专业角色 + 模块进度笔记 + 总工作记录”的协作方式。代码和可重复的验证结果是最终事实来源，文档用于记录状态、决策、依赖和交接。

## 开工前必读

1. 阅读本文件、docs/coordination/README.md 和 WORKLOG.md 的最新记录。
2. 阅读 docs/BACKLOG.md 中的任务、依赖和验收标准。
3. 阅读自己负责模块的 docs/progress 进度笔记。
4. 修改前检查 Git 状态，不覆盖其他人未完成的工作。

## 任务与状态

- 任务编号前缀：ARCH、BE、DATA、IOT、FE、QA、SEC、OPS、DOC。
- 状态只使用：待规划、就绪、进行中、阻塞、待复核、已完成、已取消。
- 任务开始时，在 docs/BACKLOG.md 登记负责人、状态和开始日期。
- 任务完成时，必须同时更新任务台账、模块进度笔记和 WORKLOG.md；缺少验证证据不得标记为已完成。

## 进度笔记规范

每次有意义的工作都要在所属模块笔记顶部追加一条，包含：

- 日期与任务 ID；
- 负责角色与当前状态；
- 已完成内容和变更文件；
- 验证命令、结果和未验证项；
- 决策、风险、阻塞与下一步；
- 对其他模块的依赖或交接内容。

WORKLOG.md 只记录可供全组快速拉取的摘要，详细过程留在模块笔记中。

## 修改与验证门禁

- 只修改当前任务需要的文件，不顺手重构无关模块。
- 不得提交密码、令牌、真实 Wi-Fi 凭据或私有网络地址。
- 接口、数据库模式或跨模块数据结构变更，必须在任务台账写明影响和交接要求。
- 至少执行与变更相匹配的静态检查、自动化测试或运行时验证。
- 不得以“理论上可行”代替实际验证；无法验证的部分必须明确标记。

## 交接规则

- 交接时在模块笔记写明：提供方、接收方、契约或文件、当前状态、验证方法和待办。
- 跨模块依赖必须在 docs/BACKLOG.md 中使用任务 ID 关联，不仅留在对话中。
- 全组会议、重要决策和里程碑记录到 WORKLOG.md。
